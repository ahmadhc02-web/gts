const { initializeApp } = require('firebase/app');
const { getFirestore, collection, query, where, getDocs, addDoc, doc, updateDoc, deleteDoc, setDoc, orderBy, getDoc, limit: fLimit } = require('firebase/firestore');

// ...
